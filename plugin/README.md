# The plugin moved

`rare-disease-consult` is now a standalone repo at:

**https://github.com/fryanpan/rare-disease-consult**

Install with `claude --plugin-dir .` after cloning that repo. The plugin is MIT-licensed (independent of this benchmark repo's CC BY-NC-SA 4.0 license).
