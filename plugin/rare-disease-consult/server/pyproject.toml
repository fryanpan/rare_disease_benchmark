[project]
name = "rare-disease-consult-server"
version = "0.1.0"
description = "HPO/Orphanet MCP server for the rare-disease-consult Claude Code plugin"
requires-python = ">=3.11"
dependencies = [
    "mcp>=1.0.0",
    "pyhpo>=4.0.0",
]
